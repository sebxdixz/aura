server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Dashboard instance should not expose public intake paths.
    location ~ ^/intake(/.*)?$ {
        return 404;
    }

    location / {
        try_files $uri /index.html;
    }
}
