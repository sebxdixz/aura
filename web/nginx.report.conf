server {
    listen 80;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # Public intake paths
    location ~ ^/intake(/.*)?$ {
        try_files /index.html =404;
    }

    location / {
        try_files $uri /index.html;
    }
}
